using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        // Vector3 mouseScreenPos = playerInput.KeyboardMouse.PointerPosition.ReadValue<Vector2>();
        // var mousePos = Camera.main.ScreenPointToRay(mouseScreenPos);
        // if (Physics.Raycast(mousePos, out RaycastHit hit, 100f, LayerMask.NameToLayer("Walkable")))
        // {
        //     pointerPos = hit.point;
        //     print(hit.transform.name);
        // }
    }
}
